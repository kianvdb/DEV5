using UnityEngine; 


// Define the EventOnlyInteractable class which inherits from the Interactable class
public class EventOnlyInteractable : Interactable
{
    // This class currently doesn't have additional functionality or properties,
    // but it is set up to represent an interactable object that triggers events when interacted with.
    // Since it inherits from Interactable, it can utilize any functionality or properties defined in the Interactable class.
}
